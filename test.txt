你好，我们严格按照贵方的接口文档进行了函数编写，目前测试问题主要有以下几个，望回复。
1、仿真程序上一共有11个功能，哪些功能我们这边不需要测试?
你们只要测试,3,4,5,6就可以了 
2、当我们测试两个制卡（公卫制卡和农合制卡）时，当你们程序调用我们函数iIOCtl后，调用完成后，你们的仿真程序会直接跳出错误对话框如附件
我们现在这个函数临时改为最简单的版本如下：
int __stdcall iIOCtl(int cmd, unsigned char *data,int datasize)
{
	//此处本来有 我们设备的蜂鸣函数调用，为了排除问题，先只对该函数直接返回
		return 0;
}
IOCtl目前没有用，蜂鸣在open时会调用，可以忽略，保留。
3、调用 PatchCard 运行到调用iOpen 完成并Return 0，后又会跳出问题二中的那个异常对话框。
这个不需要你实现。你不需要测试。

nifdafldjasfld



备注：能否提供调用我们库的源代码以便测试。
我把我们做的一个设备驱动给你吧。